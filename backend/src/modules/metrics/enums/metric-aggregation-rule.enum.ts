export enum MetricAggregationRule {
  NONE = 'none',
  SUM = 'sum',
  AVG = 'avg',
  MIN = 'min',
  MAX = 'max',
  LAST = 'last',
  COORDINATE_PAIR = 'coordinate_pair',
  COMPARE = 'compare',
  TRAJECTORY = 'trajectory',
}
